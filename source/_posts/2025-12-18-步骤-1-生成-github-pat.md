- --
title: - --
date: 2025-12-18 00:25:10
updated: 2025-12-18 00:25:10
categories: [技术分享]
tags: []
excerpt:
cover: /img/default-cover.jpg
toc: true
top: false
comments: true
description: 本文分享了技术实践和经验总结。
keywords:
reading_time: 1 分钟
- --

- --

title: 步骤 1 生成 GitHub PAT
categories:
  - DevOps
tags:
  - Git
  - GitHub
  - JSON
  - Windows
  - AI
  - Github
excerpt: "Windows 环境下 + 使用 npm 启动 GitHub MCP server 的完整配置示例，适合你在 Gemini CLI 中直接使用。
description: 本文分享了技术实践和经验总结。
keywords: JSON, DevOps, AI
reading_time: 6 分钟
- --

 \U0001F527 步骤 1：生成 GitHub PAT"
cover: /img/default-cover.jpg
toc: true
top: false
comments: true
description: 本文分享了DevOps相关的技术实践和经验总结。
keywords: 'Git, GitHub, JSON, Windows, AI'
reading_time: 2 分钟
abbrlink: 7415cf97
date: 2025-12-18 00:12:22
updated: 2025-12-18 00:12:22
- --

<!-- more -->

## 目录

* *Windows 环境下 + 使用 npm 启动 GitHub MCP server** 的完整配置示例，适合你在 Gemini CLI 中直接使用。

- --

# 🔧 步骤 1：生成 GitHub PAT

按照我之前的说明，去 GitHub 创建一个 **Fine-grained Token**（推荐），最小权限配置大致如下：

* **Repository access**：选择你要访问的仓库（可以选 All repositories 或指定某些仓库）
* **Permissions**：

* **Contents** → Read & Write（读写代码内容）
* **Issues** → Read & Write（管理 issue）
* **Pull requests** → Read & Write（管理 PR）

生成后，你会得到一个令牌，例如：

```text

ghp_abcd1234efgh5678ijkl90mnopqrstuvwx

```

- --

# 🔧 步骤 2：设置环境变量（Windows）

在 Windows PowerShell 中运行：

```powershell

setx GITHUB_PERSONAL_ACCESS_TOKEN "ghp_abcd1234efgh5678ijkl90mnopqrstuvwx"

```

设置完成后，**重新打开一个新的 PowerShell 窗口**，这样环境变量才会生效。
你可以验证是否成功：

```powershell

echo $env:GITHUB_PERSONAL_ACCESS_TOKEN

```

- --

# 🔧 步骤 3：编辑 Gemini CLI 配置

找到配置文件：

* **全局配置路径**：`%USERPROFILE%\.gemini\settings.json`
* **项目级配置路径**：`你的项目目录\.gemini\settings.json`

如果文件不存在，先创建：

```powershell

mkdir $env:USERPROFILE\.gemini
notepad $env:USERPROFILE\.gemini\settings.json

```

在 `settings.json` 中写入以下内容（完整示例）：

```json

{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}

```

- --

# 🔧 步骤 4：安装依赖

在命令行执行：

```powershell

npm install -g @modelcontextprotocol/server-github

```

这样确保 `npx` 可以找到 `@modelcontextprotocol/server-github`。

- --

# 🔧 步骤 5：验证配置

1. 启动 Gemini CLI

```powershell

   gemini

```

2. 输入：

```text

   /mcp

```

   你应该能看到 **github mcp server 已连接**。
3. 试着问它：

```text

   列出我仓库中的 open issues

```

- --

✅ 至此，Windows + npm 方式的 GitHub MCP 配置就完成了。
